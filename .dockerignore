.git
.gitignore
node_modules
server/node_modules
*.log

# Local vector store — regenerated at runtime if needed
server/.vectors

# Local API data — use a mounted volume in production
api/data

# Editor / OS
.DS_Store
*.swp
*.swo
